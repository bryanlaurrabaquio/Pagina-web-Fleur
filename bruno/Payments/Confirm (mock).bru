meta {
  name: Confirm (mock)
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/payments/confirm
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "orderId": {{orderId}},
    "sessionId": "{{sessionId}}"
  }
}
