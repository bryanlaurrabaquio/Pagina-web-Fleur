meta {
  name: Add Item
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/cart/items
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "sku": "p4",
    "quantity": 1
  }
}
